#include "main.h"

/**
 * print_last_digit - returns the last digit of a number
 * @int: takes an int value of the number
 * Return: last digit
 */

int print_last_digit(int)
{
	int lastD = int num % 10;
		return (lastD);
}
