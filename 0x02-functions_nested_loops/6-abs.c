#include "main.h"

/**
 * _abs - version of abs()
 * @n: value to get the absolute value of
 * return the absolute value
 * Return: abs(n)
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
